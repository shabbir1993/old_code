<% provide :title, "Admin" %>

<% content_for :nav_list do %>
  <%= link_to "Users", users_path, class: "list-group-item#{" active" if params[:controller].split('/').last == 'users'}" %>
  <%= link_to "Machines", machines_path, class: "list-group-item#{" active" if params[:controller].split('/').last == 'machines'}" %>
<% end %>

<%= render template: "layouts/dashboard" %>
