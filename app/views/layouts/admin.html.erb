<% provide :title, "Admin" %>

<% content_for :nav_list do %>
  <%= link_to "Import data", imports_path, class: "list-group-item#{" active" if params[:controller].split('/').last == 'imports'}" %>
  <%= link_to "Users", users_path, class: "list-group-item#{" active" if params[:controller].split('/').last == 'users'}" %>
<% end %>

<%= render template: "layouts/application" %>
