<% provide :title, "Production history" %>

<% content_for :nav_list do %>
  <%= sidebar_link_to(:grouping, 'by_day') { "Daily" } %>
  <%= sidebar_link_to(:grouping, 'by_week') { "Weekly" } %>
  <%= sidebar_link_to(:grouping, 'by_month') { "Monthly" } %>
  <%= sidebar_link_to(:grouping, 'by_quarter') { "Quarterly" } %>
<% end %>

<% content_for :search do %>
  <%= render 'search_form' %>
<% end %>

<% content_for :nav do %>
  <ul class="nav nav-tabs">
    <%= tab_link_to("Index", 'index') %>
    <%= tab_link_to("Yield", 'yield_time_series') %>
    <%= tab_link_to("Produced area", 'produced_area_time_series') %>
  </ul>
<% end %>

<%= render template: "layouts/application" %>
