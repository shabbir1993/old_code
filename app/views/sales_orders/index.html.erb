<%= render 'search_form' %>
<span>
  Orders: <strong><%= @presenter.total_orders %></strong>
  Pieces: <strong><%= @presenter.total_pieces %></strong>
  Area: <strong><%= @presenter.total_area %></strong>
</span>

<%= paginate sales_orders %>

<%= link_to_modal_new(new_sales_order_path, "Enter sales order") %>

<ol class="panel-group">
  <%= render sales_orders %>
</ol>
