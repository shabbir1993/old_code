$('#film-actions-modal').modal('hide');
<% if params[:film][:destination].present? %>
  <% @films.each do |film| %>
    $("<%= "tr#film-#{film.id}" %>").replaceWith("<%= j render 'moved_film', film: film, row_class: "info" %>");
  <% end %>
<% end %>
