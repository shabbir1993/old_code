@import "bootstrap";

body {
  min-height: 600px;
}

.sidebar-fixed {
  position: absolute;
  width: 160px;
  
  .btn {
    margin-left: 15px;
  }
}

.header.container-fluid {
  padding: 0;
  margin-bottom: 10px;
}

.main {
  margin-left: 160px;
}

.modal-body {
  padding: 0;
  max-height: 600px;
}

.modal .form-actions {
  margin-bottom: 0;
}

.modal .control-group {
  margin-bottom: 10px;
}

.control-group.hidden {
  margin: 0;
}

.inline-fields .control-group {
  display: inline-block
}

.inline-fields .control-group:not(:first-child) .controls {
  margin-left: 0
}

.new-film-link {
  padding-top: 9px;
  display: block;
  height: 30px;
  border-style: dashed;
  border-width: 1px;

  &:hover {
    background-color: #FCF8E3;
  }
}

.pagination {
  float: right;
}
