jQuery(function() {
  $('body').on('hidden', '#film-actions-modal', function () {
      $(this).removeData('modal');
  });
  $("#inventory-form").bind("ajax:success", function(event, data, status, xhr) {
    $("#edit-film-modal .modal-body").html(data);
    $("#edit-film-modal").modal('show');
  });
});
